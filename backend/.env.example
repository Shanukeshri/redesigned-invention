# Backend .env.example
# Replace values with your environment-specific ones.
MONGO_URI=mongodb://localhost:27017/renewnet
PORT=5500
JWT_SECRET=change_this_to_a_secure_random_string
JWT_EXPIRES_IN=1d
CORS_ORIGINS=http://localhost:5173
# AI base URLs: either single url or comma-separated list. If multiple, provide mapping in AI_BASE_URL_MAP as JSON string.
AI_BASE_URL_RENEWABLE=http://localhost:8000
AI_BASE_URL_CARBON=http://localhost:8001
AI_BASE_URLS=http://localhost:8000,http://localhost:8001
# Example mapping if using AI_BASE_URLS (JSON string):
# AI_BASE_URL_MAP={"renewable":"http://localhost:8000","carbon":"http://localhost:8001"}
AI_BASE_URL_MAP={"renewable":"http://localhost:8000","carbon":"http://localhost:8001"}

# Notes:
# - CORS_ORIGINS must be a comma-separated list of origins. Backend splits it into an array.
# - Do NOT prefix backend env names with VITE_ (that's for frontend). Vite loads only VITE_ envs.
