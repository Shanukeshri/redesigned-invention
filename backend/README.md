# RenewNet Backend

## Overview
Node.js + Express backend for RenewNet. Implements REST APIs, AI forwarding, seed scripts, and demo data.

## Quickstart (local)
1. Install deps:
   ```bash
   cd renewnet-backend
   npm install
