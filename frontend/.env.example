# Frontend Vite .env.example
VITE_API_BASE_URL=http://localhost:5500
VITE_AI_BASE_URL=http://localhost:8000

# Note: Vite env variables must be prefixed with VITE_ to be available to client code.
# After changing .env, restart dev server.
